When the task affects one plugin, always prefix the title with that plugin's display name in square brackets.

Examples:

- `[Notify] Fix notification delivery`
- `[Amp] Update provider settings`

Infer the plugin from the user's prompt, file paths, or current task context. Preserve the plugin's official capitalization and spacing. If the task affects the repository rather than one plugin, do not add a prefix.
