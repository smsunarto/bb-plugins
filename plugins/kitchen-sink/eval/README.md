# Historical Smart Embed evaluation

The diff-embedding experiments and scores in this directory predate the removal of diff and patch directives. The baseline prompt reflects current code and Unity citations, but the historical cases and scoring rules do not evaluate the new Last Turn inspector. Use the plugin tests and isolated app verification for the current contract.
