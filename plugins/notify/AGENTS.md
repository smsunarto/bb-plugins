# Notify

- RPC map keys and the `notify_user` tool name are a public contract. Do not rename them.
- Keep macOS delivery, thread events, settings, and command copy in lockstep.
- Delivery is server-only. Do not add a window bridge, queue, or replay path.
