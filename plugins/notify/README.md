# Notify (deprecated)

Notifications are now built into bb. The standalone Notify plugin is retired
and has been removed from this repository's plugin catalogs and release pipeline.

Use bb's built-in notification settings. If you still have the old Notify plugin
installed, disable or remove it from bb's plugin settings.
