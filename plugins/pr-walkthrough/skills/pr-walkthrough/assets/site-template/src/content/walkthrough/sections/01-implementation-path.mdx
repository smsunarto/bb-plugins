# Implementation path

- ID: `implementation-path`
- Objective: Follow the implementation in the order that makes the change easiest to verify.
- File: [src/example.ts](-) — Shows the behavior change in the sample patch.

The example changes one exported value, which demonstrates the ordered inline-diff review path.

Use inline diffs and linked evidence first. Omit diagrams when this explanation is sufficient.

## Guide

### Core behavior

The sample keeps one logical phase because both changed exports form the complete public behavior. Reading them together demonstrates the Guide contract without inventing architecture: the old value is replaced, then a second exported flag makes the new reviewed state explicit for callers.

This phase also demonstrates exact coverage. The selector owns the deleted line and both added lines once, while the compiler is free to repeat only unchanged context around them.

#### Replace the example value and expose its review state

The replacement changes the value consumers receive from the existing export. The adjacent flag is part of the same observable surface, so it belongs in this excerpt rather than an artificial second phase.

- Diff: `replace-example-value` [src/example.ts](#L1,R1-R2)
