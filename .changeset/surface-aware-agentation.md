---
"@smsunarto/bb-plugin-agentation": patch
---

Enrich annotations with the exact public bb plugin UI surface that owns the selected element, including component slots, composer contributions, and host-rendered plugin actions. Keep the global React toolbar compatible with bb's foreign-DOM mutation guard.
