---
"@smsunarto/bb-plugin-gtd-sidebar": patch
---

Show the applied GitButler virtual branch instead of `gitbutler/workspace` on thread cards. When several virtual branches are applied, show their count rather than guessing one.
